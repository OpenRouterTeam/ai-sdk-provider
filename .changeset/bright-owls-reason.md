---
'@openrouter/ai-sdk-provider': patch
---

Accept `max` in the reasoning effort type so supported models can use it through typed model settings and provider options without an `extraBody` workaround.
